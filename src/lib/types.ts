export type MemberRole = "owner" | "editor" | "viewer";

export type NodeType =
  | "income"
  | "account"
  | "saving"
  | "expense"
  | "subscription"
  | "custom";

export type FlowCycle = "monthly" | "weekly" | "daily" | "once";
